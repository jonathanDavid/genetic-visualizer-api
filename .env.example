# Copy to .env for local development.

# Host/port for uvicorn.
HOST=0.0.0.0
PORT=8000

# Comma-separated list of allowed CORS origins (the web client dev server).
CORS_ORIGINS=http://localhost:5173,http://localhost:5200,http://localhost:5201
